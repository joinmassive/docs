export const Autoscaling = () => {
  return (
    <Warning>
      This endpoint autoscales; if you receive a <code>503</code> response, maintain traffic and
      expect errors to resolve within a few minutes.
    </Warning>
  );
};
